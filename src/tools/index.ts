/**
 * Tools index
 *
 * Note: Tools are created dynamically in server.ts with runtime configuration.
 * This module exists for compatibility but exports are minimal.
 */
export type { AnyToolDefinition } from './types.js';
export { prodiscoToolMetadata } from './prodisco/index.js';

